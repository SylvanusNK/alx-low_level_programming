#include <stdio.h>
#include <stdlib.h>
/**
 * main - returns alpahbet a-z
 * Return: Always 0 (success)
*/
int main(void)
{
	char d = 'a';

	while (d <= 'z'0)
	{
		putchar(d);
		d++;
	}
	putchar('\n');

	return (0);
}

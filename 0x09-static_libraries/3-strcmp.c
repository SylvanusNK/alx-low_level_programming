#include "main.h"
#include <string.h>

/**
* _strcmp - a function that compares strings
* @s1: first string
* @s2: second string
* Return: an interger
*/

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
